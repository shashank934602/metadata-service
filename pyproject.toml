[tool.poetry]
name = "metadata-service"
version = "0.1.0"
description = "Dataset metadata service"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.110.0"
uvicorn = "^0.27.0"
sqlalchemy = "^2.0.0"
pymysql = "^1.1.0"
alembic = "^1.13.0"
python-dotenv = "^1.0.0"
cryptography = "^46.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
